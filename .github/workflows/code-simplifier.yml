---
name: Code Simplifier

on:
  schedule:
    - cron: "0 4 * * 1"
  workflow_dispatch:

permissions:
  contents: write
  id-token: write
  pull-requests: write

jobs:
  simplify:
    uses: JacobPEvans/ai-workflows/.github/workflows/code-simplifier.yml@v0.6.0
    secrets: inherit

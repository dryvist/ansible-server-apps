---
name: Post-Merge Docs Review

on:
  push:
    branches: [main]

permissions:
  contents: write
  id-token: write
  pull-requests: write

jobs:
  review:
    uses: JacobPEvans/ai-workflows/.github/workflows/post-merge-docs-review.yml@v0.3.0
    secrets: inherit

---
galaxy_info:
  author: JacobPEvans
  description: Deploy Microsoft SQL Server 2022 in Docker on an LXC container
  license: Apache-2.0
  min_ansible_version: "2.15"

  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
    - name: Ubuntu
      versions:
        - jammy
        - noble

  galaxy_tags:
    - mssql
    - sqlserver
    - docker
    - database

dependencies: []
  # community.docker collection is specified in requirements.yml

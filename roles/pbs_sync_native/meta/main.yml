---
galaxy_info:
  role_name: pbs_sync_native
  author: dryvist
  description: >-
    Configure PBS Sync via the native proxmox-backup-manager CLI with
    explicit changed_when guards. Skeleton -- implementation lands in
    a follow-up PR.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - bookworm
        - trixie
  galaxy_tags:
    - backup
    - proxmox
    - pbs

dependencies: []
